#include "logic.h"
int count_multiples_3(int number) {
	return 3 * (1 + number) * number / 2;
}

int count_multiples_5(int number) {
	return 5 * (1 + number) * number / 2;
}

int count_multiples_8(int number) {
	return 8 * (1 + number) * number / 2;
}
